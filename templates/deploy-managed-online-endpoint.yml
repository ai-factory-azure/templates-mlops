steps:
  - task: AzureCLI@2
    displayName: Deploy managed online endpoint
    inputs:
      azureSubscription: $(ml_workspace_connection)
      scriptType: bash
      scriptLocation: inlineScript
      inlineScript: |
        set -e
        ls
        pwd
        az ml endpoint create -f code/azureml/deploy_managed_online_endpoint.yml
